import { MapLocation } from '../map-data';

// Map dimensions adjusted to match other maps (2000x2000)
const MAP_DIMENSIONS = { width: 2000, height: 2000 };

// No markers for now - will be added later
export const purgatorioIslandLocations: MapLocation[] = [];
